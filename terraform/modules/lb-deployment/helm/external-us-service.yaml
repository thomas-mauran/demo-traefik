apiVersion: v1
kind: Service
metadata:
  name: us-remote-service
  namespace: default
spec:
  type: ExternalName
  externalName: api.us
  ports:
    - port: 443
      targetPort: 443
      protocol: TCP
